# Reference only. Export variables in your shell or deployment configuration.
# python app.py does not automatically read this file.
SENTINEL_HOST=127.0.0.1
PORT=5000
SENTINEL_INTEGRITY_MODE=enforce
# Empty means same-origin only. Use comma-separated exact origins if necessary.
SENTINEL_CORS_ORIGINS=
